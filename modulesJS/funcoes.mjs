export function showAge(name, age)
{
   return `The age of ${name} is ${age}`;
}

export function showCity(name, city)
{
   return `The city of ${ name } is ${ city }`;
}

export function showHobby(name, hobby)
{
   return `The hobby od ${ name } is ${ hobby }`;
}